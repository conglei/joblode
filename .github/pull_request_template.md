## What & why

<!-- What does this change, and why? Link the phase/task in docs/DESIGN.md if relevant. -->

## How it was tested

<!-- New/updated tests, and how you verified locally. -->

## Checklist

- [ ] Tests added or updated (TDD: failing test first)
- [ ] `cargo fmt --all --check` and `cargo clippy --workspace --all-targets -- -D warnings` pass
- [ ] `cargo test --workspace` passes
- [ ] `pnpm turbo run lint typecheck test build` passes
- [ ] Docs updated if behavior changed
